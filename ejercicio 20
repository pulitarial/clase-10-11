#include<stdio.h>
#include <string.h>
int t;
int i=0;
int r=0;
int main()
{
    printf("Ingrese la tabla de que numero desea que le muestre\n");
    scanf("%d",&t);
    for(i=1; i<=10;i++)
        {r= t*i;
        printf("resultado %d x %d = %d\n", t,i,r);}
	
	return 0;
}
